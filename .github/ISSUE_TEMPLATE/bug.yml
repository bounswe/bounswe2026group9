name: 🐞 Bug
description: File a bug/issue
title: "[BUG] <title>"
labels: ["Bug"]
body:
  - type: markdown
    attributes:
      value: |
        🚀 Thanks for reporting a bug! Please fill out the sections below as clearly as possible.

        ---
        Before opening an issue, make sure there isn't a duplicate.

  - type: textarea
    id: description
    attributes:
      label: Description
      description: Clearly describe the problem/bug in 2-4 sentences.
      placeholder: What is the bug? Where does it happen? Who is affected? What is the current and expected behavior?
    validations:
      required: true

  - type: dropdown
    id: reviewer
    attributes:
      label: Reviewer
      description: Who will review/verify that the fix meets the acceptance criteria?
      multiple: true
      options:
        - Emre Bektaş
        - Can Emir Bora
        - Muhittin Köybaşı
        - Mustafa Koyuncu
        - Faik İhsan Südüpak
        - İbrahim Fırat Yoğurtçu
        - Tuğçe Yücel
    validations:
      required: true

  - type: textarea
    id: acceptance_criteria
    attributes:
      label: Acceptance Criteria
      description: "The task is considered done when all items below are checked. Add/remove items as needed with using - [ ]."
      value: |
      - [ ] The issue is reproducible OR the root cause is clearly explained
      - [ ] A fix is implemented
      - [ ] Relevant tests are added/updated (if applicable)
      - [ ] The fix is verified (steps repeated, no longer occurs)
      - [ ] Any related documentation is updated (if applicable)
    validations:
      required: true


  - type: textarea
    id: anything_else
    attributes:
      label: Anything else?
      description: |
        Links? References? Anything that will give us more context about the issue you are encountering!

        Tip: You can attach images or log files by clicking this area to highlight it and then dragging files in.
    validations:
      required: false

  - type: markdown
    attributes:
      value: |
        ---
        ⚠️ Do Not Forget These While Managing the Issue! ⚠️

        * Keep the title and description clear and descriptive.
        * Avoid assigning more than 2 people unless necessary.
        * Post progress updates as comments (investigation, findings, decisions).
        * Track and respond to review comments if needed.
        * Link dependent issues using `#issue_number` (e.g., "Blocked by #12").
        * Before closing: ensure acceptance criteria is satisfied, add a final summary comment, and attach PR/commit links if applicable.
