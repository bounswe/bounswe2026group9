services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: sem-backend
    ports:
      - "8888:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend:/app
    restart: unless-stopped
